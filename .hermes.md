# Scrum Ceremony Platform — Hermes Guardrails

## Architecture Constraints (NEVER VIOLATE)

### Data Boundaries
- Product PostgreSQL owns: ceremonies, boards, votes, actions, analytics, AI outputs
- ERPNext MariaDB owns: leads, contacts, customers, invoices, sales orders
- NEVER cross-query between databases. Use the integration service.

### Anonymity Hard Rules
- Anonymous notes MUST store author_id in a SEPARATE table from display content
- The `board_items` table NEVER contains user_id when anonymity is active
- The `anonymous_author_map` table is sealed behind a service with RBAC
- NEVER log or expose anonymous author identity in API responses
- Vibe-coded code that touches anonymity MUST be human-reviewed before merge

### State Machine Rules
- Ceremony state transitions are defined in `docs/03-ARCHITECTURE/ceremony-state-machine.md`
- NEVER allow Hermes to modify FSM definitions without explicit approval
- Every state transition MUST be tested: valid transitions pass, invalid fail

### CRDT Rules
- Board sync uses Yjs ONLY. No custom conflict resolution.
- NEVER modify Yjs internals or binding adapters without review
- CRDT test suite must pass: concurrent edits, reconnect, offline

### Multi-tenancy
- EVERY database query MUST include tenant_id filter (enforced by middleware)
- Row-level security is enabled on ALL tables
- NEVER write a raw SQL query without tenant_id WHERE clause

### AI Rules
- ALL AI outputs marked "suggested" until facilitator approves
- Gemma 9B outputs validated against JSON schema before storage
- NEVER allow AI to directly create Jira tickets or modify action status
- AI service has no write path to Jira

### Integration Rules
- Jira sync operations are idempotent (idempotency key on every request)
- ERPNext sync failures go to dead-letter queue, never silently dropped
- Stripe webhook signatures ALWAYS verified before processing

---

## Session Workflows (MANDATORY for Every Task)

### START TASK — Before writing ANY code:
1. READ: Linked docs, Huly story, relevant ADR
2. SCAN: DB schema, existing dependencies, ceremony FSM if applicable
3. PLAN: Output 3-5 bullet execution plan, identify zone (GREEN/RED), WAIT for approval

### END TASK — Before declaring PR ready:
1. RUN: ruff, biome, tsc, mypy locally
2. RUN: Domain guardrails (verify_anonymity, verify_fsm, verify_yjs_schema)
3. RUN: pytest + coverage ≥80%, relevant Playwright tests
4. SELF-CORRECT: Up to 3 attempts, then STOP and report
5. PREPARE: Fill PR template completely, link Hylury story, attach screenshots

---

## Domain-Specific Guardrails (MANDATORY)

These run IN ADDITION to standard linting. They catch errors specific to our domain:

| Guardrail | Script | Catches |
|-----------|--------|---------|
| Anonymity Invariant | `scripts/verify_anonymity.py` | author_id leak in anonymous flow |
| FSM Transition Validity | `scripts/verify_fsm.py` | Invalid ceremony state transitions |
| Tenant Isolation | `scripts/verify_tenant_isolation.py` | Missing tenant_id in API queries |
| Idempotency Keys | `scripts/verify_idempotency.py` | Integration sync without idempotency |
| CRDT Schema | `scripts/verify_yjs_schema.py` | Yjs document structure violations |
| Vote Token Uniqueness | `scripts/verify_vote_tokens.py` | Vote double-casting vulnerability |

---

## Agent Skills (Autonomous Context Gathering)

I am authorized to run these read-only scripts WITHOUT asking:

| Script | Purpose |
|--------|---------|
| `scripts/agent_tools/peek_db.py [table]` | Schema + sample rows |
| `scripts/agent_tools/mock_api_response.py [endpoint]` | API structure reference |
| `scripts/agent_tools/test_ceremony_fsm.py` | FSM conformance check |
| `scripts/agent_tools/check_anonymity.py [id]` | Anonymity verification |
| `scripts/agent_tools/get_ceremony_state.py [id]` | Debug stuck ceremonies |
| `scripts/agent_tools/list_active_teams.py` | Usage pattern context |
| `scripts/agent_tools/check_sync_status.py [id]` | Integration health |
| `scripts/agent_tools/ollama_benchmark.py` | AI latency check |

---

## Vibe Coding Zones

### GREEN ZONE (Vibe-code freely)
- CRUD API endpoints (teams, templates, actions, workspaces)
- Database schema + Alembic migrations
- Auth integration (Clerk)
- ERPNext integration service
- Stripe billing integration
- Admin/settings pages
- Template library UI
- Email/notification service
- Analytics dashboard (charts)
- Docker/deployment config
- Playwright test writing

### RED ZONE (Spec-first, human review required)
- Yjs CRDT board sync logic
- Anonymity engine (dual-path data model)
- Ceremony state machine implementation
- Jira bidirectional sync
- AI output validation + structured output schemas
- Multi-tenant RLS policies
- WebSocket authentication
- Stripe webhook signature verification
- Integration idempotency + circuit breaker logic
- Any code touching `anonymous_author_map`

---

## Project Info
- Repo: https://github.com/soumeendra/scrum-ceremony-platform
- Docs: D:\scrum-ceremony-platform\docs\
- Stories: D:\scrum-ceremony-platform\stories\README.md
- Huly: Local workspace for Epic→Story→Subtask tracking
- Ground Rules: docs/10-GUARDRAILS/ground-rules.md
